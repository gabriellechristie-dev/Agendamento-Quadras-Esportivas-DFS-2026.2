generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Quadra {
  id          Int      @id @default(autoincrement())
  nome        String
  modalidade  String
  localizacao String
}